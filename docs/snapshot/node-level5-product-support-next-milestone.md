# Node Level 5 100 / 100 / 0 milestone — unverified

The previous 100 / 100 / 0 Node milestone is no longer treated as a public claim.

It is blocked on `proofs/nodejs/real-cross-arch-e2e-gate/`, which must connect every claimed supported row to retained real VM E2E artifacts in both architecture directions.

Until that gate passes, the dashboard keeps Node at `0 / 0 / 0` for public claim purposes.
