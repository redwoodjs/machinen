# 031-memory-class-instance

Selected class instance data and prototype identity materialized target-native.

This is a portability memory-state smoke row. It does not claim arbitrary Node process restore, raw V8 heap restore, same PID continuation, or active runtime resource continuation.
