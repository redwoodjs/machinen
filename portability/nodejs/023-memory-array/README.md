# 023 — memory array

Portability smoke for selected packed V8 Smi array state. The row decodes anchored memory-evidence values and materializes a target-native JS array `[1, 2]`.

Not claimed: arbitrary Node process restore, raw V8 heap restore, same PID continuation.
