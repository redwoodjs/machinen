# 022 — memory plain object

Portability smoke for selected V8 plain-object numeric state. The row decodes an anchored memory-evidence fragment and materializes a target-native JS object with the decoded `total` value.

Not claimed: arbitrary Node process restore, raw V8 heap restore, same PID continuation.
