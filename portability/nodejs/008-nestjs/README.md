# 008-nestjs

NestJS HTTP service; dependency install/build required

Disposition: `supported-with-declared-config`

This row is part of the Node portability corpus. It proves classification and, when run through `run-corpus.ts --execute-vm`, target-native execution via Machinen runtime on each requested architecture. It does not claim raw Node process or V8 heap continuation.
