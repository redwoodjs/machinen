{
  "name": "@machinen/example-pi",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bake": "MACHINEN_ASSETS_DIR=${MACHINEN_ASSETS_DIR:-../../release-assets} node --conditions=source --import tsx bake.ts",
    "start": "MACHINEN_ASSETS_DIR=${MACHINEN_ASSETS_DIR:-../../release-assets} node --conditions=source --import tsx run.ts"
  },
  "dependencies": {
    "@machinen/runtime": "workspace:*"
  }
}
