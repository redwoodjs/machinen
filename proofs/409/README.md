# Proof 409 — Node Level 5 product snapshot detection gate

This proof validates automatic Node Level 5 product snapshot detection and refusal behavior.

Run with:

```sh
pnpm exec tsx proofs/409/smoke.ts
```
