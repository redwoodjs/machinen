# Proof 243 — Node Level 5 product support 20% gate

This proof is part of the Node Level 5 product support 20% tier. It supports five declared idle Node service families and keeps broad Node product support at 0%.

Run with:

```sh
pnpm exec tsx proofs/by-id/243/smoke.ts
```
