# Proof 285 — Node Level 5 product support 65% gate

This proof is part of the Node Level 5 product support 65% tier. It supports fourteen declared service/boundary families, moves broad Node product support to 5%, and keeps arbitrary process cross-arch restore at 0%.

Run with:

```sh
pnpm exec tsx proofs/285/smoke.ts
```
