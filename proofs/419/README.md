# Proof 419 — Node Level 5 product snapshot detection gate

This proof validates automatic Node Level 5 product snapshot detection and refusal behavior.

Run with:

```sh
pnpm exec tsx proofs/419/smoke.ts
```
