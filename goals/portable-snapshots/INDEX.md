# Portable snapshots goals

Archived goals:

- First portable snapshots roadmap pass:
  `goals/archive/portable-snapshots-001-010/`

Parent roadmap issue: https://github.com/redwoodjs/machinen/issues/810

## Active goal

1. [Reconcile native process-continuation proofs into the portable snapshots roadmap](001-reconcile-native-process-continuation-proofs.md)

The active goal is intentionally a reconciliation/productization goal. The repo
already contains substantial native/process-continuation, Node, runtime, and
stateful-service proof work under the older `portable-snapshot-format` roadmap.
The next step is to inventory that work, map it into the portable snapshots
roadmap, and identify the real product gaps instead of restarting with new toy
proofs.
