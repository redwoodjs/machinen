---
"machinen": patch
---

Publish an unscoped launcher that delegates to `@machinen/cli`, enabling zero-install commands such as `npx machinen --help` and `npx machinen run machinen.dev/run/claude-code`.
