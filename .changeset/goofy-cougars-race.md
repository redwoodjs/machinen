---
"@machinen/runtime": minor
"@machinen/cli": minor
---

Add goal-driven CPU resource policies with `resources.cpu.maxVcpus`, `quotaCpus`, and `weight`, CLI flags for boot/fork, Linux cgroup v2 quota/fairness enforcement, and registry/list observability.
