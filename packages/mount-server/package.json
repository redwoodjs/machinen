{
  "name": "@machinen/mount-server",
  "version": "0.6.1",
  "private": true,
  "description": "Transport-agnostic Zig FUSE opcode handlers for machinen live mounts (#329, #332). Source-only workspace package; the `fuse` module is imported by @machinen/microvm's in-VMM virtio-fs device. The FUSE-over-vsock transport was removed in #338.",
  "license": "FSL-1.1-MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/redwoodjs/machinen.git",
    "directory": "packages/mount-server"
  },
  "scripts": {
    "build": "zig build",
    "test": "zig build test"
  }
}
