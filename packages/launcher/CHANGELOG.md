# machinen

## 0.8.8

### Patch Changes

- 95a18bd: Publish an unscoped launcher that delegates to `@machinen/cli`, enabling zero-install commands such as `npx machinen --help` and `npx machinen run machinen.dev/run/claude-code`.
  - @machinen/cli@0.8.8
