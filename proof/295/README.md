# Proof 295 — Node Level 5 product support 80% gate

This proof is part of the Node Level 5 product support 80% tier. It requires real bidirectional VM cross-architecture evidence for every supported family, moves broad Node product support to 20%, and keeps arbitrary process cross-arch restore at 0%.

Run with:

```sh
pnpm exec tsx proof/295/smoke.ts
```
